---
type: object
required:
- id
- key
- release
properties:
  id:
    $ref: '#/definitions/id'
  key:
    $ref: '#/definitions/string'
  release:
    $ref: '#/definitions/release'

definitions:
  id:
    type: integer
  string:
    type: string
  boolean:
    type: boolean

  release:
    type: object
    required:
    - id
    - key
    properties:
      id:
        $ref: '#/definitions/id'
      key:
        $ref: '#/definitions/string'
